# Phase 7 Tenure/Relocation RED Gate

The branch intentionally contains failing tenure behavior tests before `HousingTenureSystem` exists. The expected failure is limited to `tests/housing-tenure-relocation.test.ts`; the pre-existing suite should remain green. Production implementation follows only after observing this failure in pull-request CI.
