name: Stack 3 Task 2 Assets (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Task 2 write runner retired; asset sources are committed directly.'
