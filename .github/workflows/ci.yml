name: Phase 0B Validation Fast Path Method Profile

on:
  pull_request:

permissions:
  contents: read

jobs:
  profile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Profile validation-fast-path entity synchronization
        run: node --experimental-strip-types --test tests/phase0b-validation-fastpath-method-profile.test.ts
