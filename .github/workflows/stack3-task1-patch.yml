name: Stack 3 Task 1 Patch (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Task 1 patch runner retired after GREEN patch.'
