{
  "name": "civic-foundry",
  "version": "0.7.0-metropolitan",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "for i in 1 2 3 4 5; do node --experimental-strip-types --test --test-name-pattern='performance: 5000 active economy ticks remain an interactive diagnostic' tests/phase6-headless.test.ts; done",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.json && cp index.html dist/index.html && cp src/styles.css dist/styles.css",
    "dev": "python -m http.server 5173 -d dist",
    "test:smoke": "python tests/smoke/phase6_smoke.py",
    "test:smoke:phase7": "python tests/smoke/phase7_land_housing_smoke.py"
  }
}
